[build]
  command = "node source/build.js"

# Publish directory and functions are set per-site in the Netlify dashboard:
#   Operator site  →  publish: Deploy-operator   functions: Deploy-operator/netlify/functions
#   VC site        →  publish: Deploy-vc          functions: Deploy-vc/netlify/functions
